name: CI

on:
  pull_request:
  push:
    branches:
      - main

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    name: ${{ matrix.os }} / Node ${{ matrix.node }}
    runs-on: ${{ matrix.os }}
    timeout-minutes: 10
    strategy:
      fail-fast: false
      matrix:
        os:
          - ubuntu-latest
          - macos-latest
        node:
          - '20'
          - '22'

    steps:
      - name: Check out repository
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4

      - name: Set up Node.js
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with:
          node-version: ${{ matrix.node }}

      - name: Run lint and tests
        env:
          # Only macOS runners get a disposable Keychain; opt in there so the
          # real-Keychain integration tests still run in CI. Left unset (empty)
          # on ubuntu, where `security` doesn't exist, so those tests stay skipped.
          CLAUDE_ROTATOR_REAL_KEYCHAIN: ${{ matrix.os == 'macos-latest' && '1' || '' }}
        run: npm run check
