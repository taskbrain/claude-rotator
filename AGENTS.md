# AGENTS.md

## 作業方針

- 1ブランチ・1PR・1目的を守る。実装前に目的、対象内、対象外、検証方法を短く示す。
- 依頼されたユーザーフローに必要な最小変更だけを行う。別機能、別OS、別サブシステムの問題はFollow-upとして報告し、同じPRで直さない。
- 想定範囲が別モジュールへ広がる、またはファイル数・所要時間が概ね倍になる場合は、編集を止めてユーザー承認を得る。
- リポジトリ全体の同系統監査、将来向けの抽象化、依頼外のセキュリティ強化は明示依頼がある場合だけ行う。
- シークレット、OAuth token、認証JSON、authorization codeをargv、fixture、log、error、文書へ入れない。

## 設計とレビュー

- 既存フローのbounded変更はチャット内の短い設計で進め、仕様書や詳細計画ファイルを自動作成しない。
- 新規サブシステム、公開インターフェース変更、データ移行、複数の独立成果物がある場合だけ文書化されたspec／planを作る。
- 通常変更の独立レビューは1回とする。指摘修正後は該当箇所だけ再確認し、新しい全体監査を開始しない。
- Ultraレビューは認証・秘密情報・不可逆操作・データ消失・本番移行など、変更箇所が高影響境界を直接扱う場合だけ使う。

## 検証

- 変更中は関連するfocused testを実行する。小修正ごとにfull matrixを繰り返さない。
- PR前に `npm run check` を1回実行する。macOS／Ubuntu、Node 20／22の全組合せはGitHub Actionsを正本とする。
- ローカルでCI相当の隔離が必要な場合はDockerを使う。例:

  `docker run --rm --network none -v "$PWD:/app:ro" -w /app node:22-bookworm npm run check`

- `npm run dev` は実行しない。変更が影響しないE2E、複数OS、fault injectionを儀式的に追加しない。
- 完了報告には実行したコマンド、成功／失敗件数、未実行項目と残リスクを短く記載する。

## Git

- 署名や自動生成の共同著者表記を追加しない。
- unrelated changesをstageしない。push、PR、merge前に対象diffとbranchを再確認する。
